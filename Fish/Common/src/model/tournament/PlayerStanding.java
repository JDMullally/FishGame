package model.tournament;

/**
 * Represents a Player's current standing in a tournament.  They were are either caught cheating,
 * eliminated, or are still playing.
 */
public enum PlayerStanding {
    CHEATER,
    ELIMINATED,
    REMAINING
}
